@model MusicApp.Models.User

<div>
    <h4>User</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.UserName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserName)
        </dd>

    </dl>
</div>
<p>
    @using MusicApp.Helpers
    @Html.ActionLink("Edit", "UpdateUser", new { id = UrlEncryptionHelper.EncryptId(Model.UserId) }) |
    @Html.ActionLink("Delete", "DeleteUser", new { id = UrlEncryptionHelper.EncryptId(Model.UserId) }) |
    @Html.ActionLink("Back", "GetUsers")
</p>
